use std

fn some_fn(){
    a = 2
    if a == 2
        ret;
    printf("heyo!\n")
}

fn main() {
    some_fn()
    //Both produce the same type
    my_fn1|(none fn int)
    my_fn2|(fn int)
    ret 0
}

