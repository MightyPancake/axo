use std
use io
use maps

fn main([][]byte args){
    e = error{}
    e.msg = "hello"
    
    e_ptr = e@
    e_ptr.msg
    
    e_ptr2 = e_ptr@
    e_ptr2.msg

    printf("%s\n", e_ptr2.msg)
    
    ret 0
}
