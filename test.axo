use std
use io

fn main([][]byte args) {
    fl|file
    if (args.len[0] < 2){
        printf("Provide a filename!\n")
        exit(1)
    }
    fl ?= io.open(args[1].data)!$oops()
    printf("File %s opened!\n", args[1].data)
    ret 0
}

